/**
 * __ProjectName__ — the sample public API shipped with this template.
 * Replace it with your real implementation and re-export the public surface here.
 */
export { greet } from "./greeter.js";
